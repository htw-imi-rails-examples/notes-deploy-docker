default: &default
  adapter: postgresql
  encoding: unicode
  host: db
  username: postgres
  password:
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: notes_development

test:
  <<: *default
  database: notes_test

production:
  <<: *default
  host: localhost
  database: notes_production
